Set a config entry

## Description

Set a rig configuration entry.

The argument is a `key=value` pair, e.g. `rig config set mode=user`. The
configuration file is created if it does not exist yet. See
`rig config --help` for the known keys, and `rig config list` for the
entries that are already in the configuration file.
