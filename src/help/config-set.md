Set a config entry

## Description

Set a rig configuration entry.

The argument is a `key=value` pair, e.g. `rig config set mode=user`. The
configuration file is created if it does not exist yet. Use
`rig config list` to see the available keys.
