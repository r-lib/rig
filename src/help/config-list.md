List the names of all config entries

## Description

List the names of all rig configuration entries.

Use `rig config get <key>` to see the value of an entry, and
`rig config set <key>=<value>` to change it.
