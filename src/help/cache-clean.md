Delete cached files

## Description

Deletes files from rig's cache directory. With no options, deletes the
whole cache. Use `--category` to delete only one category of cached files
(`binaries`, `built`, `packages` or `metadata`), leaving the rest in place.
See [`rig cache info`](cache.qmd#rig-cache-info) for what each category
contains.
