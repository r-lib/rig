Add a new library

## Description

Add a new user package library for the current R version.

The new library is created empty. Adding a library does not change the
default library; use `rig library default` to switch to it. No
administrator rights are needed.

Use `--r-version` to add a library for another installed R version,
instead of the default one.
