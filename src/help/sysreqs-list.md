## Description

List the system libraries and tools that rig can install (experimental,
macOS only).

See `rig sysreqs info <name>` for details about a specific tool, and
`rig sysreqs add <name>` to install one.
